=image_tag @pin.image.url(:medium)
%h1= @pin.title
%p= @pin.content
%small
	Posted by
	=@pin.user.email
-if user_signed_in? && current_user.id == @pin.user_id
	%p
		=link_to "Edit", edit_pin_path(@pin), class: "btn btn-warning"
		=link_to "Delete", @pin, method: :delete, class: "btn btn-danger"